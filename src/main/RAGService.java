package main;

public class RAGService implements AIService {

    // This is where your AI code will go, i just have that below so i could test my part
    public String evaluate(Phone phone) {
        phone.setEstimatedPrice(299.99);
        return "Good condition — this is a placeholder result until the real AI is connected.";
    }
}
